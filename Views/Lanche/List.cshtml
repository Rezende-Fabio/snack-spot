@model IEnumerable<snack_spot.Models.Lanche>

<div>
    <h2>@ViewData["Titulo"]</h2>
</div>

@foreach( var lanche in Model){
    <div>
        <h4>@lanche.Nome</h4>
        <img src="@lanche.ThumbnailUrl">
        <h3>Preço: @lanche.Preco.ToString("c")</h3>
    </div>
}