@model Lanche

<div class="col-card col-sm-3 col-md-3 col-lg-3 text-center mb-5 me-3">
    <h4>@Model.Nome</h4>
    <img src="@Model.ThumbnailUrl"><br>
    <span class="col-preco">Preço: @Model.Preco.ToString("c")</span>
</div>